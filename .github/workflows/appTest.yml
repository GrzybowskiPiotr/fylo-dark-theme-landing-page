name: AppTest
run-name: App Test
on: [push]
jobs: 
    App build:
        runs-on: ubuntu-latest
        steps: 
            - uses: actions/checkout@6
            - name: Use Node.js
              uses: actions/setup-node@v7
              with: 
                node-version: '20.x'
            - run: npn ci
            - run: build --if-present  

        